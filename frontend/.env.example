# zkLogin Configuration

# Google OAuth Client ID (Required)
# Get this from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# Sui Network URL (Optional - defaults to devnet)
VITE_SUI_NETWORK_URL=https://fullnode.devnet.sui.io

# Redirect URI (Optional - defaults to window.location.origin)
# Must match the authorized redirect URI in your OAuth app configuration
VITE_REDIRECT_URI=http://localhost:5173

# ZK Prover Service URL (Optional - defaults to Mysten Labs prover)
VITE_PROVER_URL=https://prover-dev.mystenlabs.com/v1

# Salt Service URL (Optional - implement your own backend for production)
# Leave empty to use Mysten Labs salt service (for development only)
VITE_SALT_SERVICE_URL=

# Backend URL for Enoki gas sponsorship
# **REQUIRED IN PRODUCTION** - Must point to your deployed backend
# Development default: http://localhost:3000
VITE_BACKEND_URL=http://localhost:3000

# Deployed Suitter contract IDs
VITE_PACKAGE_ID=0x...
VITE_REGISTRY_ID=0x...

# Sui network
VITE_SUI_NETWORK=devnet
